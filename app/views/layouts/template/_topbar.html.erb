<!-- Topbar -->
        <nav class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow">

          <!-- Sidebar Toggle (Topbar) -->
          <button id="sidebarToggleTop" class="btn btn-link d-md-none rounded-circle mr-3">
            <i class="fa fa-bars"></i>
          </button>

          <!-- Topbar Search -->
         

          <!-- Topbar Navbar -->
          <ul class="navbar-nav ml-auto">          
            <div class="topbar-divider d-none d-sm-block"></div>
            
            <!-- Nav Item - User Information -->
            <li class="nav-item dropdown no-arrow">
              <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button"
                                data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <div style="size: 10px;">
                                  <font size="2"  color="grey"><%= "#{current_user_name} / " "#{current_nombre_area_controller}"%></font>                                  
                                  <br>
                                  <font size="2" color="grey"><%=current_nombre_area_empresa_controller%></font>
                                </div>

                               <!-- <span class="mr-2 d-none d-lg-inline text-gray-600 small"><%= "#{current_user_name} / " "#{current_nombre_area_controller}" %></span> -->
                                <img class="img-profile rounded-circle"
                                    src="<%= current_user.persona.foto.url%>">
                            </a>
              
              <!-- Dropdown - User Information -->
              <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">               
                <%= link_to ver_perfil_path(current_user.persona), class: "dropdown-item" do %>
                  <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
                  Perfil
                <%end%>  

                <%= link_to mostrar_parametro_path(:id => current_user.id), class: "dropdown-item" do %>
                  <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
                  Parámetro
                <%end%>                

                <div class="dropdown-divider"></div>              
                <%= link_to modal_salir_path, :remote => true, data: {'toggle' =>  "modal",'target' => '#modal-content-cerrar-sesion'}, class: "dropdown-item" do %>
                  <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                  Cerrar Sesión
                <%end%>  
              </div>
            </li>

          </ul>

        </nav>
        <!-- End of Topbar -->
<div id="modal-content-cerrar-sesion" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-ssm" role="document">
    <div class="modal-content">
    </div>
  </div>
</div>
<div id="modal-content-busqueda-codeqr" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel"
    aria-hidden="true"></div>
    